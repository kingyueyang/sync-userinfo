module_name = "base, receiver, mysql";

config = {
    base = ( {
            level = "DEBUG";}
           );

    receiver = ( {
            server_ip = "0.0.0.0";
            server_port = 8080;
            dump_file = "/tmp/community_sync_sql.dump";}
            );

    mysql = ( {
            ip = "192.168.142.10";
            user = "ci_user";
            passwd = "y4hrthjUggg";
            db = "ci";
            port = 3306;
            thread_count = 1;}
            );
}
